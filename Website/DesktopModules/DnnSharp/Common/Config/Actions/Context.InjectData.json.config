[
    {
        "Id": "InjectData",
        "TypeStr": "DnnSharp.Common.Actions.Context.InjectData, DnnSharp.Common.Actions",
        "Title": {
            "default": "Inject Data"
        },
        "HelpText": {
            "default": "This action lets you add data (tokens) manually to the current execution context.<br/>You can use the data afterwards using the token syntax, for example [Name].<br/>Note that this is only valid for current action list. It does not persists until next list of actions executes for example on submit."
        },
        "Settings": {
            "Group": "Context"
        },
        "Parameters": [
            {
                "Id": "Data",
                "Type": "NameValueCollection",
                "Title": {
                    "default": "Data"
                },
                "Settings": {
                    "NameColumn": "Name",
                    "ValueColumn": "Value",
                    "ButtonName": "Add Data"
                },
                "HelpText": {
                    "default": "Input the Name and Value for each data you want to inject. It can be accessed later by using [NameHere] tokens. Note that if data with same name already exists, it will get overwritten. Can contain context tokens (for example [TokenName]) and <a href=\"http://www.dnnsharp.com/dnn/modules/my-custom-tokens\" target=\"_blank\" tabindex=\"-1\">My Tokens</a>."
                }
            }
        ]
    }
]